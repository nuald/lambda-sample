akka {
  remote {
    log-remote-lifecycle-events = off
    netty.tcp {
      hostname = "127.0.0.1"
      port = <port>
    }
  }

  cluster {
    seed-nodes = ["akka.tcp://ClusterSystem@<host>:2551"]
    roles = [<role>]
  }
}
